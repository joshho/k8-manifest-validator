apiVersion: example.com/v2
kind: Comprehensive
metadata:
  name: valid-embedded-resource
spec:
  embeddedField:
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: embedded-deployment
  requiredField: required-value
  choiceField: option-a
  logicalField: prefix-test
  v2OnlyField: v2-only
