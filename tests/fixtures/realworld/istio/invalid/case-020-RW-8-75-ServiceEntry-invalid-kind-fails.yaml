apiVersion: networking.istio.io/v1alpha3
kind: BadServiceEntry
metadata:
  name: wrong-kind
  namespace: default
spec:
  hosts:
  - example.com
  ports:
  - number: 80
    name: http
    protocol: HTTP
  location: MESH_EXTERNAL
  resolution: DNS