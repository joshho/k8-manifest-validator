apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: no-port
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      name: http
      protocol: HTTP
    hosts:
    - example.com