apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: port-missing-number
  namespace: default
spec:
  hosts:
  - example.com
  ports:
  - name: http
    protocol: HTTP
  location: MESH_EXTERNAL
  resolution: DNS