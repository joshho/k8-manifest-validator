apiVersion: networking.istio.io/v1alpha3
kind: BadGateway
metadata:
  name: wrong-kind
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - example.com