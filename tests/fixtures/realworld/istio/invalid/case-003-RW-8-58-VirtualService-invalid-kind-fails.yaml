apiVersion: networking.istio.io/v1alpha3
kind: BadVirtualService
metadata:
  name: wrong-kind
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - route:
    - destination:
        host: reviews