apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: no-hosts
  namespace: default
spec:
  ports:
  - number: 80
    name: http
    protocol: HTTP
  location: MESH_EXTERNAL
  resolution: DNS