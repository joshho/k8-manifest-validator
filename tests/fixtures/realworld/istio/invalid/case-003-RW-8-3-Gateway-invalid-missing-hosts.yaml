apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: invalid-gateway-missing-hosts
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 8080
      name: http
      protocol: HTTP
