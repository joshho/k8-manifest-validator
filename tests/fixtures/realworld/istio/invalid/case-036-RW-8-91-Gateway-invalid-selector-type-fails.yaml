apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: invalid-selector
  namespace: istio-system
spec:
  selector: []
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - example.com