apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: empty-hosts
  namespace: default
spec:
  hosts: []
  ports:
  - number: 80
    name: http
    protocol: HTTP
  location: MESH_EXTERNAL
  resolution: DNS