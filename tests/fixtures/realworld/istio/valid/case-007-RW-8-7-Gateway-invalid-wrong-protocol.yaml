apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: invalid-gateway-wrong-protocol2
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 8080
      name: http
      protocol: NOT_A_PROTOCOL
    hosts:
    - "*"
