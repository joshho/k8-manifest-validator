apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: http2-gw
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 8080
      name: http2
      protocol: HTTP2
    hosts:
    - "http2.example.com"