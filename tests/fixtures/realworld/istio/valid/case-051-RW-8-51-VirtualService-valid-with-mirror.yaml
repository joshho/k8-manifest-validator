apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: mirror-route
  namespace: default
spec:
  hosts:
  - ratings
  http:
  - route:
    - destination:
        host: ratings
        subset: v1
    mirror:
      host: ratings
      subset: v2