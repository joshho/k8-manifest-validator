apiVersion: networking.istio.io/v1alpha3
kind: Sidecar
metadata:
  name: selector-sidecar
  namespace: default
spec:
  workloadSelector:
    labels:
      app: my-app
  egress:
  - hosts:
    - "default/*"