apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: tcp-pool-dr
  namespace: default
spec:
  host: reviews
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 100