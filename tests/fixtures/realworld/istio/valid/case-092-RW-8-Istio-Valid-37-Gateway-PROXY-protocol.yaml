apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: proxy-gw
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: proxy
      protocol: PROXY
    hosts:
    - "proxy.example.com"