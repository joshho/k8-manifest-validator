apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: delay-vs
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - fault:
      delay:
        percent: 10
        fixedDelay: 5s
    route:
    - destination:
        host: reviews
        subset: v1