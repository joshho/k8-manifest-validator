apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: http-res-svc
  namespace: default
spec:
  hosts:
  - http-svc
  ports:
  - number: 8080
    name: http
    protocol: HTTP
  resolution: HTTP