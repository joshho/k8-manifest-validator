apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: append-headers
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - route:
    - destination:
        host: reviews
        subset: v1
    appendHeaders:
      x-custom-header: value