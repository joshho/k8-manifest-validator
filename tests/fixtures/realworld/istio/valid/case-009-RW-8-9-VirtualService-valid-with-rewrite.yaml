apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: rewrite-route
  namespace: default
spec:
  hosts:
  - details
  http:
  - rewrite:
      uri: /newdetails
    route:
    - destination:
        host: details
        subset: v1