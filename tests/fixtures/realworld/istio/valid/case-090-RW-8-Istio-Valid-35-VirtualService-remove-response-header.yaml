apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: rm-header
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - route:
    - destination:
        host: reviews
        subset: v1
    removeResponseHeader: x-removed