apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: mirror-vs
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - route:
    - destination:
        host: reviews
        subset: v1
    mirror:
      host: reviews
      subset: v2
    mirrorPercent: 50