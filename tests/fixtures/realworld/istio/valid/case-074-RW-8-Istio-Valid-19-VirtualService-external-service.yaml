apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: external-vs
  namespace: default
spec:
  hosts:
  - httpbin.org
  http:
  - route:
    - destination:
        host: httpbin.org