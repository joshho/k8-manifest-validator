apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: direct-response-route
  namespace: default
spec:
  hosts:
  - ratings
  http:
  - directResponse:
      body:
        string: "200 OK"
      status: 200
    route:
    - destination:
        host: ratings