apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: dns-svc
  namespace: default
spec:
  hosts:
  - dns.example.com
  ports:
  - number: 8080
    name: http
    protocol: HTTP
  location: MESH_EXTERNAL
  resolution: DNS