apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: set-header
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - route:
    - destination:
        host: reviews
        subset: v1
    setResponseHeader:
      x-added: value