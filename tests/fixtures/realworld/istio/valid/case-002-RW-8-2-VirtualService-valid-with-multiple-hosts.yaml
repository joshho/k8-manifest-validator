apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: multi-host-route
  namespace: default
spec:
  hosts:
  - productpage
  - details
  - reviews
  http:
  - route:
    - destination:
        host: productpage
        subset: v1