apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: tcp-gw
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 9000
      name: tcp
      protocol: TCP
    hosts:
    - "tcp.example.com"