apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: match-route
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - match:
    - headers:
        end-user:
          exact: jason
    route:
    - destination:
        host: reviews
        subset: v2