apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: delegate-route
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - delegate:
      name: reviews-delegate
      namespace: default