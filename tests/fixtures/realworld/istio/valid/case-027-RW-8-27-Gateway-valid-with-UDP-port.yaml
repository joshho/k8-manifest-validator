apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: udp-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 53
      name: dns-udp
      protocol: UDP
    hosts:
    - "*.example.com"