apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: invalid-gateway-wrong-protocol
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 8080
      name: http
      protocol: HTTP_PROXY
    hosts:
    - "*"
