apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: external-route
  namespace: default
spec:
  hosts:
  - external.example.com
  http:
  - route:
    - destination:
        host: external.example.com
        port:
          number: 80