apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: redirect-route
  namespace: default
spec:
  hosts:
  - reviews
  http:
  - redirect:
      authority: new-reviews.example.com