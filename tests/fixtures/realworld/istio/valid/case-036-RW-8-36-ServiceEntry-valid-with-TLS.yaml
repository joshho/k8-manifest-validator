apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: tls-svc
  namespace: default
spec:
  hosts:
  - tls-example.com
  ports:
  - number: 443
    name: https
    protocol: HTTPS
  location: MESH_EXTERNAL
  resolution: DNS