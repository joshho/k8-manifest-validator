apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana-invalid-config-numbers
  namespace: monitoring
spec:
  version: "11.2.0"
  config:
    server:
      http_port: 3000