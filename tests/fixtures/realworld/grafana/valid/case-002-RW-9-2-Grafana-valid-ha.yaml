apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: ha-grafana
  namespace: monitoring
spec:
  replicas: 3
  version: "10.2.0"
  config:
    security:
      secretName: grafana-secret
    ingress:
      className: nginx
