apiVersion: redisilient.github.com/v1
kind: RedisCluster
metadata:
  name: my-redis-cluster
  namespace: default
spec:
  kubernetesConfig:
    image: redis:7.0
  clusterSize: 3
  redisConfig:
    maxmemory: 2gb
    maxmemoryPolicy: allkeys-lru
    appendonly: "yes"