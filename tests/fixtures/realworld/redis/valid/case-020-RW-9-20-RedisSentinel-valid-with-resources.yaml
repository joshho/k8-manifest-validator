apiVersion: redisilient.github.com/v1
kind: RedisSentinel
metadata:
  name: my-redis-sentinel
  namespace: default
spec:
  kubernetesConfig:
    image: redis:7.0
    resources:
      limits:
        cpu: "1"
        memory: 1Gi
      requests:
        cpu: "500m"
        memory: 512Mi