apiVersion: redisilient.github.com/v1
kind: Redis
metadata:
  name: my-redis
  namespace: default
spec:
  kubernetesConfig:
    image: redis:7.0
    resources:
      limits:
        cpu: "2"
        memory: 2Gi
      requests:
        cpu: "1"
        memory: 1Gi