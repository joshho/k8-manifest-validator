apiVersion: argoproj.io/v1alpha1
kind: ArgoCD
metadata:
  name: argocd
  namespace: argocd
spec:
  server:
    replicas: 2
    insecure: false
    host: argocd.example.com
  repo:
    replicas: 2
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
  controller:
    replicas: 1