apiVersion: elasticsearch.k8s.elastic.co/v1
metadata:
  name: elasticsearch-bad
  namespace: elastic-system
spec:
  replicas: 3
  version: "8.12.0"
  nodeSets:
  - name: master
    count: 3
    roles:
    - master
    storage:
      size: 10Gi
