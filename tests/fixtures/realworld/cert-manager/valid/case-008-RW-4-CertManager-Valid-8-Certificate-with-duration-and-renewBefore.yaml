apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: duration-cert
  namespace: default
spec:
  secretName: duration-tls
  issuerRef:
    name: my-issuer
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - duration.example.com