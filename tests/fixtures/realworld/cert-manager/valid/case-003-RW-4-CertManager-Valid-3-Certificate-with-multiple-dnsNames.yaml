apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: multi-dns-cert
  namespace: default
spec:
  secretName: multi-dns-tls
  issuerRef:
    name: my-issuer
  dnsNames:
    - example.com
    - www.example.com
    - api.example.com