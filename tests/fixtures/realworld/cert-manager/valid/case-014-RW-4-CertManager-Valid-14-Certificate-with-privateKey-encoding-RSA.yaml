apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pk-cert
  namespace: default
spec:
  secretName: pk-tls
  issuerRef:
    name: my-issuer
  privateKey:
    encoding: PKCS1
    algorithm: RSA
    size: 2048
  dnsNames:
    - pk.example.com