apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: template-cert
  namespace: default
spec:
  secretName: template-tls
  issuerRef:
    name: my-issuer
  secretTemplate:
    labels:
      environment: production
      team: platform
  dnsNames:
    - template.example.com