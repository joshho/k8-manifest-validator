apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nc-email-cert
  namespace: default
spec:
  secretName: nc-email-tls
  issuerRef:
    name: my-issuer
  nameConstraints:
    permitted:
      email:
        - example.com
  dnsNames:
    - nc-email.example.com