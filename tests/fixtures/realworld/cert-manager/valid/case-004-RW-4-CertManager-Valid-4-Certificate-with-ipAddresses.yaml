apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ip-cert
  namespace: default
spec:
  secretName: ip-cert-tls
  issuerRef:
    name: my-issuer
  ipAddresses:
    - "10.0.0.1"
    - "192.168.1.1"