apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: my-cert-cn
  namespace: default
spec:
  secretName: my-cert-tls
  issuerRef:
    name: my-issuer
  commonName: example.com
  dnsNames:
    - example.com