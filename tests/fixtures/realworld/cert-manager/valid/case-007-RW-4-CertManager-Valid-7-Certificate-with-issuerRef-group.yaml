apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: group-issuer-cert
  namespace: default
spec:
  secretName: group-issuer-tls
  issuerRef:
    name: my-issuer
    kind: Issuer
    group: cert-manager.io
  dnsNames:
    - group.example.com