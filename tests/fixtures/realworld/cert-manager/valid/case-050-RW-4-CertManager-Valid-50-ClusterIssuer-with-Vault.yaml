apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: cluster-vault
spec:
  vault:
    server: https://vault.example.com:8200
    path: pki/certs
    auth:
      tokenSecretRef:
        name: vault-token
        key: token