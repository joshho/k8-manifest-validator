apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wrong-pk-size-type
  namespace: default
spec:
  secretName: my-cert-tls
  issuerRef:
    name: my-issuer
  privateKey:
    size: "2048"