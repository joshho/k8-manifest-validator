apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: issuerref-non-object-name
  namespace: default
spec:
  secretName: my-cert-tls
  issuerRef:
    name:
      first: value
  dnsNames:
    - example.com