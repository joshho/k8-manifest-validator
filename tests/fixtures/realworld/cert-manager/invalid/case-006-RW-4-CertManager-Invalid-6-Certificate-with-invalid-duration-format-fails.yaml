apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bad-duration
  namespace: default
spec:
  secretName: my-cert-tls
  issuerRef:
    name: my-issuer
  duration: invalid-duration
  dnsNames:
    - example.com