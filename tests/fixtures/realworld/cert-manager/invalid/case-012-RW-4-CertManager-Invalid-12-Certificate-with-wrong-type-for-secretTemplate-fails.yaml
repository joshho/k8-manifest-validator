apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wrong-template-type
  namespace: default
spec:
  secretName: my-cert-tls
  issuerRef:
    name: my-issuer
  secretTemplate: labels
  dnsNames:
    - example.com