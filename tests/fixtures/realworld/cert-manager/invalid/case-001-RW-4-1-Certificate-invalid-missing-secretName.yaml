apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: invalid-cert-missing-secret
  namespace: default
spec:
  issuerRef:
    name: letsencrypt-prod
    kind: Issuer
    group: cert-manager.io
  dnsNames:
    - example.com
