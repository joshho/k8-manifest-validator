apiVersion: cert-manager.io/v1
kind: CertificateRequest
metadata:
  name: csr-usages-cert
  namespace: default
spec:
  issuerRef:
    name: my-issuer
  request: TESTREQUESTBASE64==
  usages:
    - digital signature
    - key encipherment
    - server auth