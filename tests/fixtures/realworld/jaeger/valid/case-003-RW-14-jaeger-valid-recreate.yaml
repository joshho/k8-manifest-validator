apiVersion: jaegertracing.io/v1
kind: Jaeger
metadata:
  name: jaeger-recreate
  namespace: observability
spec:
  strategy: recreate
  collector:
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
