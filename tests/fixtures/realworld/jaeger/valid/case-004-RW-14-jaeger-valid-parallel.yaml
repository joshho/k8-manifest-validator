apiVersion: jaegertracing.io/v1
kind: Jaeger
metadata:
  name: jaeger-parallel
  namespace: observability
spec:
  strategy: parallel
  collector:
    replicas: 2
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
