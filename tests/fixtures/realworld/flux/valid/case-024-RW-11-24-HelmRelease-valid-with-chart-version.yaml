apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nginx-version
  namespace: default
spec:
  interval: 5m0s
  chart:
    spec:
      chart: nginx
      version: "9.0.0"
      sourceRef:
        kind: HelmRepository
        name: bitnami