apiVersion: strimzi.io/v1beta3
kind: Kafka
metadata:
  name: my-cluster
  namespace: kafka
spec:
  kafka:
    replicas: 3
  zookeeper:
    replicas: 3
    resources:
      limits:
        cpu: "1"
        memory: 2Gi