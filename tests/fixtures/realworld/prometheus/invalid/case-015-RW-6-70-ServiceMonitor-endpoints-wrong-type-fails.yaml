apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: my-service-monitor
  namespace: monitoring
spec:
  jobName: http-metrics
  endpoints: "web"
  selector:
    matchLabels:
      tier: api