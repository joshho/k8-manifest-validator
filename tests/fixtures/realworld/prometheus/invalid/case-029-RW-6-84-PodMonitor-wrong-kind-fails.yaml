apiVersion: monitoring.coreos.com/v1
kind: BadPodMonitor
metadata:
  name: my-pod-monitor
  namespace: monitoring
spec:
  jobName: pod-metrics
  podMetricsEndpoints:
  - port: metrics
  selector:
    matchLabels:
      app: myapp