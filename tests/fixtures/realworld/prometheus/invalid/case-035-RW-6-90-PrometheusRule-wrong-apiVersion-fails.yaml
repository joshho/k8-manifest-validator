apiVersion: monitoring.coreos.com/v1beta1
kind: PrometheusRule
metadata:
  name: my-prometheus-rule
  namespace: monitoring
spec:
  groups:
  - name: example
    rules:
    - alert: ExampleAlert
      expr: up == 0