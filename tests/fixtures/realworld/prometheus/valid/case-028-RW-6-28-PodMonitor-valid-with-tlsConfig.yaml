apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: my-pod-monitor
  namespace: monitoring
spec:
  jobName: pod-metrics
  podMetricsEndpoints:
  - port: metrics
    tlsConfig:
      insecureSkipVerify: false
  selector:
    matchLabels:
      app: myapp