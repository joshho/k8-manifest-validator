apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: my-pod-monitor
  namespace: monitoring
spec:
  jobName: pod-metrics
  podMetricsEndpoints:
  - port: metrics
    scrapeTimeout: 10s
  selector:
    matchLabels:
      app: myapp