apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: my-prometheus-rule
  namespace: monitoring
spec:
  groups:
  - name: example
    interval: 30s
    rules:
    - alert: InstanceDown
      expr: up == 0
      for: 5m