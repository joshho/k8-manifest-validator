apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: my-pod-monitor
  namespace: monitoring
spec:
  jobName: pod-multi-metrics
  podMetricsEndpoints:
  - port: metrics
    interval: 30s
  - port: web
    interval: 15s
  selector:
    matchLabels:
      app: myapp