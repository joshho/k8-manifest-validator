apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: my-pod-monitor
  namespace: monitoring
spec:
  jobName: pod-metrics
  podMetricsEndpoints:
  - port: metrics
    path: /custom/path
  selector:
    matchLabels:
      app: myapp