apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: my-pod-monitor
  namespace: monitoring
spec:
  jobName: ""
  podMetricsEndpoints:
  - port: metrics
  selector:
    matchLabels:
      app: myapp