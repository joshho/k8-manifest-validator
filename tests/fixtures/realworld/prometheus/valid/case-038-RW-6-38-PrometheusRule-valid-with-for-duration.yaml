apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: my-prometheus-rule
  namespace: monitoring
spec:
  groups:
  - name: example
    rules:
    - alert: InstanceDown
      expr: up == 0
      for: 10m
      labels:
        severity: critical